<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="nanodegree.annekenl.walk360">

    <application
        android:name=".Walk360Application"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".activity_tracking.ActivityDetectionIntentService"
            android:exported="false" />

        <receiver android:name="nanodegree.annekenl.walk360.activity_tracking.ActivityTransitionBroadcastReceiver"
            android:exported="false">
        </receiver>

        <receiver android:name="nanodegree.annekenl.walk360.alarm_manager.MyAlarmReceiver"
            android:exported="false">
        </receiver>

    </application>
    <uses-permission android:name="com.google.android.gms.permission.ACTIVITY_RECOGNITION" />
    <uses-permission android:name="android.permission.VIBRATE" />

</manifest>